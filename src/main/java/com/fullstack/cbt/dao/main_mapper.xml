<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fullstack.cbt.dao.MainDAO">

	<select id="cbtTestList" resultType="com.fullstack.cbt.dto.TestDTO">
		SELECT su_name, tt_times, mb_id, tt_end_date, tt_score, tt_status 
			FROM cbt_subject NATURAL JOIN cbt_test ORDER BY tt_idx DESC LIMIT 5 OFFSET 0
	</select>

</mapper>